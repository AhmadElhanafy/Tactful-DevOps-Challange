apiVersion: v1
kind: Service
metadata:
  name: result
  namespace: app
spec:
  type: ClusterIP
  selector:
    app: result
  ports:
    - port: {{ .Values.result.targetPort }}
      targetPort: {{ .Values.result.targetPort }}